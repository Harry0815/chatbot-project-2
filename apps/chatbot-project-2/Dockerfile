# This file is generated by Nx.
#
# Build the docker image with `npx nx docker-build chatbot-project-2`.
# Tip: Modify "docker-build" options in project.json to change docker build args.
#
# Run the container with `docker run -p 3000:3000 -t chatbot-project-2`.
FROM docker.io/node:lts-alpine

ENV HOST=0.0.0.0
ENV PORT=3000

WORKDIR /app

RUN addgroup --system chatbot-project-2 && \
          adduser --system -G chatbot-project-2 chatbot-project-2

COPY dist/apps/chatbot-project-2 chatbot-project-2/
RUN chown -R chatbot-project-2:chatbot-project-2 .

# You can remove this install step if you build with `--bundle` option.
# The bundled output will include external dependencies.
RUN npm --prefix chatbot-project-2 --omit=dev -f install

CMD [ "node", "chatbot-project-2" ]
